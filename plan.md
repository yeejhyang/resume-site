# 八股背诵系统 · 重构计划（plan.md）

> 目标：把现有的「八股背诵工作台」重构成一个 **Vue 3 组件化 SPA**，交互更人性化、代码更好维护，同时**不引入构建工具、不增加复杂度**。

---

## 一、现状盘点

### 已有内容（数据源）
| 分组 | 文件 | 说明 |
|------|------|------|
| 技术类别 | `技术类别-01~05-*.html` ×5 | 大模型后训练 / Agent 面试题 / Agent 工程 / RAG 检索 / Java 后端 / 数据库 |
| 项目 | `01-CodeForge-Agent引擎.html` / `02-EVIDE-*.html` / `03-RAGent-*.html` | 3 个简历项目 |
| 面试 | `00-面试自我介绍.html` | 背诵稿 |
| 其他 | `resume-2.html` / `assets/` | 简历、静态资源 |

### 现有系统能力（index.html 单文件 SPA，原生 JS）
- ✅ 左侧目录树（分类 → 文件 → section 三级）＋ 右侧内容区
- ✅ Q&A 折叠展开、全展开 / 只看问题
- ✅ 掌握度标记（🟢熟了 / 🟡生疏 / 🔴不会）+ 进度统计
- ✅ 抽背盲答模式
- ✅ 🎲 随机抽题、全局搜索
- ✅ 问题增删改 + 新增（写回源 HTML）
- ✅ section 拖拽排序 + 改名（写回源 HTML）
- ✅ 数据持久化：localStorage（掌握度/折叠状态）+ server.js（问题/section 写回源文件）

### 核心痛点
1. **代码不可维护**：`index.html` 单文件 1000+ 行原生 JS，DOM 操作与业务逻辑耦合，改一个功能容易连带出错（已多次出现）。
2. **数据脆弱**：后端靠正则/平衡标签定位源文件里的 `.qa` 块，遇到特殊结构（带 id 的 section、嵌套）容易定位失败。
3. **交互不够人性化**：删除用浏览器 `confirm`、改名用 `prompt`，体验割裂；目录树层级深、状态记忆不足。
4. **依赖本地服务**：`file://` 打不开，必须 `node server.js`。

---

## 二、目标架构

```
┌─────────────────────────────────────────────┐
│          八股背诵系统（Vue 3 SPA）            │
├─────────────────────────────────────────────┤
│  入口：index.html（Vue3 CDN，无需构建）        │
│                                             │
│  ┌──────────┐  ┌─────────────────────────┐  │
│  │ 目录侧栏   │  │ 内容区（Q&A 卡片）        │  │
│  │ 分类树    │  │  · 问题折叠/展开          │  │
│  │ ·搜索框  │  │  · 掌握度标记            │  │
│  │ ·进度    │  │  · 编辑/删除/新增         │  │
│  │ ·拖拽排序│  │  · 盲答模式              │  │
│  │ ·改名    │  │  · 随机抽题              │  │
│  └──────────┘  └─────────────────────────┘  │
│                                             │
│  ┌─────────────────────────────────────┐    │
│  │ 全局：Toast / 弹窗 / 底部浮动工具栏     │    │
│  └─────────────────────────────────────┘    │
├─────────────────────────────────────────────┤
│  数据层：server.js（API） + localStorage     │
└─────────────────────────────────────────────┘
```

### 技术选型
- **前端**：Vue 3（CDN 引入，`vue.global.prod.js`），**不引入构建工具**，保留 `node server.js` 静态服务。
- **状态管理**：Vue 3 `reactive` + 模块化 store（无需 Pinia，保持简单）。
- **样式**：保留现有 CSS 变量体系，微调为组件级 `<style scoped>` 或统一样式表。
- **后端**：**复用并小幅增强**现有 `server.js`，不动数据结构。

---

## 三、模块拆分（Vue 组件化）

为避免过度设计，拆为 **6 个核心组件**（都在 index.html 内以 `<script type="module">` + 模板字符串 或 内联模板实现，不拆多文件以降低复杂度）：

| 组件 | 职责 | 关键改进 |
|------|------|---------|
| `Sidebar` | 左侧目录树 + 搜索 + 进度 | 分类/文件/section 三级可折叠，搜索实时过滤，进度实时刷新 |
| `DocView` | 右侧内容区 Q&A 卡片 | 折叠/展开、掌握度、编辑/删除/新增、盲答模式 |
| `MasteryBar` | 每题掌握度三选一 | 更清晰的状态反馈 |
| `EditModal` | 新增/编辑问题的弹窗 | 标题 + HTML 答案，**带预览** |
| `QuizMode` | 抽背盲答 | 一键隐藏答案、逐题模式 |
| `TopBar` | 顶部工具栏 | 随机、全展开/收起、盲答开关 |

---

## 四、功能清单（人性化优化）

### A. 保留并优化现有功能
1. **目录树**：三级结构，点击展开/收起，拖拽排序（手柄），改名（内联输入替代 prompt）
2. **Q&A 卡片**：点击问题折叠/展开，答案默认收起
3. **掌握度**：🟢/🟡/🔴 三态 + 目录圆点联动 + 全局进度条
4. **盲答模式**：隐藏答案，可逐题对照
5. **随机抽题**：一键随机跳到某题并高亮
6. **全局搜索**：文件名 + section + 题目文本，实时过滤

### B. 新增人性化体验
1. **编辑弹窗带实时预览**：左侧编辑 HTML，右侧预览渲染效果，所见即所得
2. **删除二次确认用自定义弹窗**：替代浏览器 `confirm`，更美观统一
3. **改名内联编辑**：在目录里直接点改名，输入框就地出现，回车保存、ESC 取消
4. **拖拽排序即时反馈**：拖拽中显示占位高亮，松手自动写回
5. **掌握度按钮悬停提示**：清楚说明"熟了/生疏/不会"含义
6. **空状态引导**：目录无搜索结果 / 文件加载失败时给出友好提示
7. **快捷键**：`1/2/3` 快速标记掌握度，`[` `]` 上一题/下一题（可选，简单实现）
8. **进度成就提示**：背完一个 section 时 Toast 提醒

### C. 数据与持久化
- 掌握度 / 折叠状态 / 盲答开关 → localStorage
- 问题增删改、section 排序/改名 → server.js API 写回源文件
- **启动时提示**：若用 `file://` 打开，检测到无法 fetch 时给出"请运行 node server.js"的友好引导

---

## 五、开发步骤

### 阶段 1：基础框架搭建
- [ ] 用 Vue 3 CDN 重构 index.html 骨架
- [ ] 建立数据 store（目录结构、当前文件、掌握度、展开状态）
- [ ] 实现 `Sidebar` + `DocView` 静态渲染（fetch 解析源文件 → 展示）

### 阶段 2：核心背诵功能
- [ ] Q&A 折叠/展开、全展开/只看问题
- [ ] 掌握度标记 + 目录圆点 + 进度统计
- [ ] 盲答模式 + 随机抽题

### 阶段 3：编辑管理功能
- [ ] 新增/编辑问题弹窗（含实时预览）
- [ ] 删除问题（自定义确认弹窗）
- [ ] 目录 section 拖拽排序 + 内联改名

### 阶段 4：体验打磨
- [ ] 全局搜索（含按需 fetch）
- [ ] 快捷键、Toast 反馈、空状态
- [ ] 响应式布局适配
- [ ] `file://` 打开时的友好引导

### 阶段 5：联调与部署
- [ ] 全流程端到端测试（Playwright）
- [ ] 提交 Git + 推送 GitHub

---

## 六、风险与取舍

| 取舍 | 说明 |
|------|------|
| 不引入构建工具 | 用 Vue CDN，保持 `node server.js` 就能跑，零安装 |
| 不拆多文件 | 所有组件放一个 index.html，虽文件变大但避免构建复杂度，权衡后取"单体 SPA" |
| 后端结构不变 | 复用现有 server.js API 与源 HTML 结构，迁移成本最低 |
| Vue 3 CDN 需联网 | 首次需从 CDN 加载 Vue；可选本地 vendor 文件保证离线 |

---

## 七、验收标准
1. 所有现有功能（背诵/掌握度/盲答/随机/搜索/增删改/排序）在重构后**不丢失**
2. 交互更人性化：弹窗替代 confirm/prompt、实时预览、拖拽反馈、快捷键
3. 代码可维护性显著提升（组件化，单文件职责清晰）
4. 用 `node server.js` 启动后全流程可用
5. 通过 Playwright 端到端测试
